# 插件部署与回归验收 · 2026-09-17

## 结论

**本轮回归通过，可继续在测试环境使用。** 已激活 Alibaba `1.3.1-qwen.2`，宿主为 NewAPI `v1.0.0-rc.37`，主程序、镜像、前端均未修改。完成前已备份旧插件和数据库，备份仅保存在测试服务器，不进入仓库。

源码 SHA-256：`c2fd801fc9ed555fc6cd9c342ebf8c0873cd1e6628d3bf2858629348fcd3c701`。

## 功能与延迟

调用路径为客户端 → NewAPI → 百炼工作空间官方接口。每个成功用例生成一张图片；本轮没有重复提交或自动重试。

| 模型 | 场景 | 结果 | 完整调用耗时 | 按测试价格应计金额 |
|---|---|---|---:|---:|
| Qwen Image 3.0 | 1K 文生图，兼容接口 | 成功 | 29.38 秒 | ¥0.18 |
| Qwen Image 3.0 | 1K 参考图，兼容接口 | 成功 | 27.89 秒 | ¥0.20 |
| Qwen Image 3.0 | 2K 异步出图 | 成功 | 45.16 秒 | ¥0.18 |
| Qwen Image 3.0 Pro | 1K 文生图，兼容接口 | 成功 | 36.03 秒 | ¥0.25 |
| Qwen Image 3.0 Pro | 1K 参考图，兼容接口 | 成功 | 70.84 秒 | ¥0.27 |
| Qwen Image 3.0 Pro | 2K 异步出图 | 成功 | 72.19 秒 | ¥0.50 |

另有 4 个负向用例：两款模型分别提交 n=7 与错误 Base64 图片，全部 HTTP 400、钱包净扣费为零。错误 Base64 返回插件的 `QWEN_INVALID_IMAGE` 提示，证明新增校验在真实运行时生效。

两款模型的异步任务完成后各额外查询 3 次，新增扣费均为零。

耗时包含测试程序的少量管理接口开销；异步耗时包含轮询等待与重复查询。样本仅 6 次成功请求，没有同条件原厂直连对照，不能据此计算插件额外延迟、P95、并发容量或速度改善。Pro 约 71–72 秒的等待仍需客户端妥善提示。本轮未重新下载图片做视觉质量检查。

## 计费核对

- 6 次成功请求，返回 usage 计算值与钱包 quota 扣减逐笔一致。
- 独立查询得到完整的 6 条消费日志；日志净扣费、钱包变化一致；任务内保存的 usage 与 HTTP 返回一致。
- 测试价格合计 **¥1.58**；按整数 quota 折回人民币 **¥1.5800120**，差额 ¥0.0000120 为逐笔整数取整，不是多扣一张图。
- 测试限定固定矩阵、不自动重试，保守费用上界 ¥2.80；共享测试令牌仍保留 ¥20 应急硬限额，不能将矩阵上界误称为令牌硬限额。
- 以上证明“返回用量 → 测试价格公式 → NewAPI 日志与钱包”一致。**未核对供应商现金账单、免费额度、优惠抵扣或实际支付，因此不声称百炼账单已对平。**

## 离线验证与清理

- 149 个独立用例通过 Node 验证；同一组 149 用例此前已通过与测试实例相同镜像的 NewAPI 插件运行时验证，不计为 298 个不同用例。
- 独立发布仓库重新执行 149 用例通过，生成索引并通过官方索引检查。
- 临时渠道、令牌均删除；两款模型原价格配置恢复；新插件仍激活且运行状态 registered。
- GitHub 自动检查仅运行离线用例，不使用账号，不产生模型调用费。

## 未解决及未覆盖

只修改插件，不修复宿主界面的“耗时 0.0s”“计费无匹配结果”、零余额鉴权提示，以及宿主超时恢复和退款对账机制。上游非 2xx 错误可能绕过插件错误处理。没有进行高并发、长时间稳定性、供应商故障注入或全部参数组合的收费重测；不能将本轮通过等同生产全量验收。

升级失败时可重新激活已备份的 `1.3.1-qwen.1`；不要在未完成任务存在时切换。此次部署发现上传版本后需要单独执行“激活”，已修正部署流程并验证活动源码哈希。
